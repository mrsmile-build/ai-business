// Integrated AI Business Document Generator
window.docGenerator = {
  renderReport(prospectData, companyInfo) {
    const title = prospectData.title || "Signal Customer Report";
    const dateStr = new Date().toLocaleDateString();
    
    return `
      <div id="doc-preview" class="p-6 bg-white text-slate-800 rounded-lg shadow-md max-w-3xl mx-auto my-4 font-sans">
        <div class="border-b-2 border-slate-900 pb-4 mb-4 flex justify-between items-center">
          <div>
            <h1 class="text-2xl font-bold text-slate-900">${title}</h1>
            <p class="text-xs text-slate-500">Generated by AI Business • ${dateStr}</p>
          </div>
          <div class="text-right text-xs text-slate-600">
            <p class="font-semibold">${companyInfo.name || "AI Business User"}</p>
            <p>${companyInfo.email || ""}</p>
          </div>
        </div>
        
        <div class="mb-4">
          <h2 class="text-md font-bold text-slate-800 border-b pb-1 mb-2">Executive Summary</h2>
          <p class="text-sm text-slate-700">${prospectData.summary || "High-priority prospect identified via AI Lead Finder signal analysis."}</p>
        </div>

        <div class="mb-4">
          <h2 class="text-md font-bold text-slate-800 border-b pb-1 mb-2">Prospect Breakdown</h2>
          <table class="w-full text-left text-xs border-collapse">
            <thead>
              <tr class="bg-slate-100 border-b">
                <th class="p-2">Business Name</th>
                <th class="p-2">Signal Score</th>
                <th class="p-2">Recommended Action</th>
              </tr>
            </thead>
            <tbody>
              <tr class="border-b">
                <td class="p-2 font-medium">${prospectData.businessName || "Target Prospect"}</td>
                <td class="p-2 text-emerald-600 font-bold">${prospectData.score || "92/100"}</td>
                <td class="p-2">${prospectData.action || "Automated WhatsApp Follow-Up"}</td>
              </tr>
            </tbody>
          </table>
        </div>

        <div class="mt-6 pt-4 border-t text-center">
          <button onclick="window.print()" class="px-4 py-2 bg-indigo-600 text-white rounded text-xs font-semibold hover:bg-indigo-700 print:hidden">
            📥 Print / Download PDF
          </button>
        </div>
      </div>
    `;
  }
};
